<p style="color: green"><%= notice %></p>

<h1 align="center"> DocSearth</h1>

<h1>Categories:</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= link_to category.title, category_path(category.id) %>
    <p>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>
